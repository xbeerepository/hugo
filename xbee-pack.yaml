schema-version: 1.0


builder:
  origin: ./hugo-builder
  var:
    version: 0.88.1


var:
  app: /app
  user: default

volumes:
  - app:{% app %}:.
directory: "{% app %}"
user: "{% user %}"


provision:
  - copy:
      builder: hugo
      todir: /usr/local/bin


command:
  hugo: hugo {{ .args }}